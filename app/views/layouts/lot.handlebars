{{> header}}
    <div class="container main-content">
        <div class="row">
            {{#with artworkstuff}}
                <div class="col-12 col-lg-6 lot-description">
                    <h2 class="item-title">{{title}}</h2><p class="lot-number" lot-number="{{lot_number}}">Lot {{lot_number}}</p>
                    <img src="{{images.[0]}}" class="item-img">
                    <p><span class="bold-header">By:</span> {{artist}}, {{year}}</p>
                    <p><span class="bold-header">FAIR MARKET VALUE:</span> ${{fmv}}</p>
                    <p><span class="bold-header">Dimensions:</span> {{dimensions}}</p>
                    <p><span class="bold-header">Materials:</span> {{materials}}</p>
                    <p><span class="bold-header">Edition:</span> {{edition}}</p>
                    <p><span class="bold-header">Details:</span> {{{detail_desc}}}</p>
                    <hr>
                    <p>To place a bid, please submit your payment information by clicking the button below.</p>
                    <form class="bid-form" action="">
                        <label for="bid-amount"><b>Bid Amount: $</b></label><input type="number" id="bid-amount" lot-number="{{lot_number}}" {{#if this.max}} min="{{add this.max this.bid_increments}}" {{else}} min="{{this.starting_bid}}" {{/if}} step="{{this.bid_increments}}" {{#if this.max}} placeholder="{{add this.max this.bid_increments}} {{else}} placeholder="{{this.starting_bid}}" {{/if}} "><button class="submit-bid" type="submit">Submit Bid</button>
                    </form>
                </div>
            {{/with}}
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="http://localhost:8000/socket.io/socket.io.js"></script>
    <script type="text/javascript" src="/js/socketIOBid.js"></script>

{{> footer}}
